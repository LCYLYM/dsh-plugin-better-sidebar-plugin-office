schema: 1
requires_model_turn: false
web:
  ready:
    path: /manifest.webmanifest
    timeout_seconds: 120
  assertions:
    - id: dsh-web-shell
      method: GET
      path: /manifest.webmanifest
      status: 200
      body_includes: DeepSeek
