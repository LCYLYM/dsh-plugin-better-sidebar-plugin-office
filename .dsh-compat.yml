schema: 1
watch:
  channel: latest
repair:
  dsh_version: 0.1.1-rc.1
plugin:
  workspace: .
  package_json: package.json
  profile: web
smoke:
  contract: compatibility/dsh-smoke.yml
gates:
  repository:
    - pnpm test
    - pnpm build
    - pnpm pack --dry-run
delivery:
  mode: direct-push
