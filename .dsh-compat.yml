schema: 1
repair:
  dsh_version: 0.1.1-rc.2
plugin:
  workspace: .
  package_json: package.json
  profile: web
smoke:
  contract: compatibility/dsh-smoke.yml
gates:
  repository:
    - pnpm test
    - pnpm build
    - pnpm pack --dry-run
delivery:
  mode: pull-request
